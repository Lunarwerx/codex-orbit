.env
.env.*
*.env
credentials.toml
credentials.toml.enc
*.secret
*.secrets
*.key
*.pem
*.p12
*.pfx
*.keystore
.secrets
secrets/
config/secrets.yml
config/secrets.yaml
config/database.yml
config/master.key
node_modules/
__pycache__/
*.pyc
npm-debug.log*
yarn-debug.log*
yarn-error.log*
dist/
build/
.next/
.nuxt/
out/
!stable/codex_assets/out/
!stable/codex_assets/out/extension.js
*.vsix
# ...but DO keep the numbered wrapper builds + the build log visible. The build
# NUMBER in the filename is how we know a build actually happened and which is
# newest. (The huge upstream Codex .vsix files stay ignored.)
!builds/codex-orbit-build-*.vsix
!builds/BUILD_LOG.md
!latest/codex-orbit.vsix
.tmp/
.DS_Store
Thumbs.db
.vscode/settings.json
.vscode/paramount.json
.idea/
*.log
logs/
# Runtime output: the pasted console log and the webview debug dump (it carries
# machine paths and chat-thread titles from other projects, so it stays local).
/logs
/codex-orbit-debug.json
# Internal AI-ops docs stay local, off the public tree (see 57093f0).
/AGENTS.md
/JUSTIFY_EXISTENCE.md
/CLAUDE.md
/docs/guides/
